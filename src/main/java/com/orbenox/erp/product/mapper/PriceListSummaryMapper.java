package com.orbenox.erp.product.mapper;

import com.orbenox.erp.product.entity.Product;
import com.orbenox.erp.product.summary.PriceListSummary;
import com.orbenox.erp.product.summary.ProductSummary;
import org.mapstruct.Mapper;
import org.mapstruct.MappingTarget;

import java.util.List;

@Mapper(componentModel = "spring")
public interface PriceListSummaryMapper {
//    PriceListSummary toDto(Product entity);
//    Product toEntity(PriceListSummary dto);
//    List<PriceListSummary> toDtoList(List<Product> entityList);
//    void updateEntityFromDto(PriceListSummary dto, @MappingTarget Product entity);
}
